package enums;

public enum Metric {
  average_commit, average_issue_opened_time, average_pull_request_merge_time, average_commit_per_developer_ratio, average_opened_to_closed_issue_ratio, num_of_releases;
}
